# Shell scripts MUST keep LF endings. With CRLF, Unix fails to run them at all:
#   /usr/bin/env: 'bash\r': No such file or directory
* text=auto
*.sh text eol=lf
*.ps1 text eol=crlf

# The seed is a compressed pg_dump -- never touch its bytes.
*.dump binary
