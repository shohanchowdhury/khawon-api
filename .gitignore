.env
.venv/
__pycache__/
*.pyc
*.db

.pytest_cache/

# Ad-hoc dumps stay out of git, but the committed seed is the exception --
# it is how a new machine gets a catalogue (see docs/ONBOARDING.md).
*.dump
!seed/khawon-seed.dump
